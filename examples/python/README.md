# Python Example

Simple Python calculator with pytest tests and flake8 linting.

## Structure

- `app.py` - Calculator functions
- `test_app.py` - Pytest tests
- `requirements.txt` - Dependencies
- `forge.yaml` - FORGE pipeline config

## Run

```bash
forge-cli run --file examples/python/forge.yaml
```
